import React, { FC } from 'react';
import * as Styled from './styles';

export const Navbar: FC = () => {
  return (
    <Styled.Root>
      <h1>Sudoku</h1>
      <div>contents</div>
    </Styled.Root>
  );
};
